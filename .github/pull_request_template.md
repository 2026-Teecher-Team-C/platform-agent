## 무엇을 바꿨나


## 왜


## 확인한 것
- [ ] `uv run --group dev pytest` (또는 `docker compose -f compose.dev.yml` 로 동작 확인)
- [ ] OS 의존 코드(`src/agent/platform/`)를 바꿨다면 macOS·Windows CI 결과 확인
- [ ] proto submodule을 올렸다면 서버 레포의 어느 `proto-v*` 태그인지 본문에 적기
